<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        ">


    <!--自定义realm-->
    <bean id="customRealm" class="com.cskaoyan.shiro.CustomRealm"></bean>

    <!--securityManager 安全管理器-->
    <bean id="securityManager" class="org.apache.shiro.web.mgt.DefaultWebSecurityManager">
        <property name="realm" ref="customRealm"></property>
        <!--注入缓存管理器-->
        <property name="cacheManager" ref="cacheManager"></property>
        <!--注入session管理器-->
        <property name="sessionManager" ref="sessionManager"></property>

    </bean>

    <!--web.xml 中 shiro的filter对应的bean-->
    <!--shiro 的 web过滤器-->
    <bean id="shiroFilter" class="org.apache.shiro.spring.web.ShiroFilterFactoryBean">

        <property name="securityManager" ref="securityManager"></property>

        <!--loginUrl认证提交地址，如果没有认证将会请求此地址进行认证，请求此地址将由
        formAuthenticationFilter进行表单认证-->
        <property name="loginUrl" value="/"></property>

        <!--认证成功统一跳转到first.action，建议不配置，shiro认证成功自动到上一个请求路径-->
        <property name="successUrl" value="home"></property>

        <!--通过unauthorizedUrl指定没有权限操作时跳转页面-->
        <property name="unauthorizedUrl" value="/refuse.jsp"></property>

        <!--过滤器链定义，从上向下顺序执行，一般将/**放在最下边-->
        <!--shiro提供了很多filter，可以构成一个filterChain-->
        <property name="filterChainDefinitions">
            <value>
                <!--/** = authc-->

                <!-- 对静态资源设置匿名访问 -->
                /image/** = anon
                /css/** = anon
                /js/** = anon

                <!-- 验证码，可匿名访问 -->
                /validatecode.jsp = anon
                /login.jsp = anon
                /ajaxLogin = anon

                <!--配置授权操作的方法有3种，这里选择第1种：-->
                <!--（1）在xml文件中配置授权操作-->

                <!--add使用注解完成-->
                <!--/employee/add = perms[employee:add]-->
                /employee/edit = perms[employee:edit]
                /employee/delete_batch = perms[employee:delete]

                /department/add = perms[department:add]
                /department/edit = perms[department:edit]
                /department/delete_batch = perms[department:delete]

                /unqualify/add = perms[unqualify:add]
                /unqualify/edit = perms[unqualify:edit]
                /unqualify/delete_batch = perms[unqualify:delete]




                <!--（2）也可以通过注解进行授权操作：通过注解时需要配置xml文件，导依赖，并且需要另配失败转向页面-->
                <!--（3）在jsp页面中添加标签-->
                <!--使用shiro，可以增加一个注销的过滤器-->
                /logout = logout

                /** = authc


            </value>
        </property>
    </bean>

    <!-- 缓存管理器 -->
    <bean id="cacheManager" class="org.apache.shiro.cache.ehcache.EhCacheManager">
        <property name="cacheManagerConfigFile" value="classpath:shiro-ehcache.xml"/>
    </bean>
    <!-- 会话管理器 -->
    <bean id="sessionManager" class="org.apache.shiro.web.session.mgt.DefaultWebSessionManager">
        <!-- session的失效时长，单位毫秒 ，这里设置为10分钟-->
        <property name="globalSessionTimeout" value="600000"/>
        <!-- 删除失效的session -->
        <property name="deleteInvalidSessions" value="true"/>
    </bean>

    <!--增加使用注解进项权限管理功能-->
    <bean id="lifecycleBeanPostProcessor" class="org.apache.shiro.spring.LifecycleBeanPostProcessor"></bean>
    <bean class="org.springframework.aop.framework.autoproxy.DefaultAdvisorAutoProxyCreator"
          depends-on="lifecycleBeanPostProcessor" />
    <bean class="org.apache.shiro.spring.security.interceptor.AuthorizationAttributeSourceAdvisor">
        <property name="securityManager" ref="securityManager"/>
    </bean>

    <!-- 配置异常跳转页面，此处异常页面是使用shiro注解时没有权限访问的跳转页面，不配置则会报500错误 -->
    <bean class="org.springframework.web.servlet.handler.SimpleMappingExceptionResolver">
        <property name="exceptionMappings">
            <props>
                <prop key="org.apache.shiro.authz.UnauthorizedException">
                    <!-- 没有权限时跳转的页面 -->
                    /refuse
                </prop>
            </props>
        </property>
    </bean>


</beans>